<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.royal.mapper.LoginMapper">

	<select id="selectStudentByOpenid" resultType="com.royal.entity.Student">
		select * from student where s_openid = #{openid}
	</select>
	
	<select id="selectTeacherByOpenid" resultType="com.royal.entity.Teacher">
		select * from teacher where t_openid = #{openid}
	</select>

</mapper>
